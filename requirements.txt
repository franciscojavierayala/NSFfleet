# Core Frameworks (Con límites de versión mayor para evitar cambios de arquitectura)
torch>=2.0.0,<3.0.0
numpy>=1.24.0,<2.0.0
pandas>=2.0.0,<3.0.0

# Librerías con historial de inestabilidad de API (Pin exacto)
nflows==0.14

# Ciencia de datos y visualización (Versión mínima garantizada)
scikit-learn>=1.3.0
matplotlib>=3.7.0
folium>=0.14.0

# Ecosistema Streamlit (Sensible a cambios de UI, mejor acotar)
streamlit>=1.28.0,<2.0.0
streamlit-folium>=0.15.0
streamlit-searchbox>=0.1.6

# Utilidades de red y geografía
requests>=2.31.0
geopy>=2.4.0